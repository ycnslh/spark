{
  "name": "spark",
  "version": "2.0.0",
  "description": "Web interface for sending Wake-on-LAN packets to your devices",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "migrate": "node scripts/migrate-csv-to-sqlite.js",
    "test": "node --test 'tests/**/*.test.js'",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0",
    "csv-parse": "^5.5.6",
    "express": "^4.21.0",
    "express-rate-limit": "^7.4.0",
    "helmet": "^8.0.0",
    "pino": "^9.5.0"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "pino-pretty": "^11.3.0",
    "prettier": "^3.3.0",
    "supertest": "^7.0.0"
  }
}
